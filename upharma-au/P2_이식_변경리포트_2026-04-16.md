# 2공정 UI/UX 이식 변경 리포트 (v2.3 — Stage2 FOB 백엔드 백업에서 이식)

**1차 이식:** 2026-04-16 (목) 19:35 KST — 팀원 싱가포르 코드 그대로 이식
**2차 호주화:** 2026-04-16 (목) 20:00 KST — SGD→AUD, GST 9%→10%, ALPS→PBS/Chemist Warehouse
**3차 GST 정밀화:** 2026-04-16 (목) 20:30 KST — 메인 GST 표시 분리 + 품목별 자동 전환
**4차 업로드 UI 통일:** 2026-04-16 (목) 21:00 KST — 직접 입력 탭에 파일 업로드 추가 + /api/p2/upload 스텁
**5차 Stage2 백엔드 이식:** 2026-04-16 (목) 21:30 KST — 백업 폴더 → 작업 폴더 stage2 API 이식 (`/api/stage2/seeds`, `/api/stage2/calculate`)
**작업자:** AI (Jisoo 요청)
**원칙:** 팀원 화면 구조는 100% 동일하게, 내용만 호주 기준으로 치환.

---

## 1. 이식 출처

- **원본 깃허브:** `Biya121/1st_logic_initial` (싱가포르 수출 프로젝트)
- **접근 방법:** `git clone` (웹페치는 차단되어 있었음)
- **참고 커밋:**
  - `3a888e1` — feat: 2공정 AI 파이프라인 + UI 전면 개선
  - `7cf6b14` — feat: 2공정 수출 전략 전면 재구성 + PDF 생성

> ✅ **v2.0 업데이트:** 싱가포르 참조 전부 호주로 치환 완료. 화면 구조/디자인/레이아웃은 팀원 원본과 100% 동일.

---

## 2. 변경된 파일 (3개)

이식 대상 폴더: `Australia_1st_logic/upharma-au/`

| 파일 | 이전 | 이후 | 변경 방식 |
|---|---|---|---|
| `templates/index.html` | 268줄 | 445줄 | 2공정 "준비 중" 자리표시를 팀원 2공정 블록(178줄)으로 교체 |
| `static/styles.css` | 932줄 | 1,350줄 | 파일 끝에 2공정 전용 CSS 블록(418줄) 추가 |
| `static/app.js` | 944줄 | 1,653줄 | 파일 끝에 호환 shim(매개층) + 팀원 JS 원본(666줄) 추가 |

백업 파일: `static/app.js.bak.before-p2-inject` (JS 수정 전 원본, 복원용)

---

## 3. 파일별 상세 변경

### 3.1 `templates/index.html`

**변경 위치:** 2공정 페이지 섹션

**이전 (8줄):**
```html
<!-- ══════════ 2공정 (준비 중) ══════════ -->
<div id="p2" class="page">
  <div>준비 중입니다</div>
</div>
```

**이후 (185줄):**
- 팀원 원본 `<div id="p2" class="page">` 블록 178줄 그대로 삽입
- 위쪽에 7줄짜리 출처 주석 추가 (싱가포르 원본 이식 사실 명시)

**삽입된 주요 UI 구성요소:**
- 탭 전환 (AI 파이프라인 ↔ 직접 입력)
- 보고서 드롭다운 선택
- PDF 파일 업로드 영역
- 공공/민간 시장 선택 버튼
- AI 가격 분석 실행 버튼
- 진행 상태 카드
- 결과 카드 5개:
  - 추출 가격 정보 (제품명, 참조가, 수출 적합성 판정, 환율)
  - 최종 산정가 · 산정 공식
  - 가격 시나리오 (3종)
  - 산정 이유
  - 보고서 다운로드

**그대로 유지된 싱가포르 문구:**
- `SGD —` (초기 플레이스홀더)
- "공공 시장: ALPS 조달청 채널 · 27개 공공기관 통합구매 기준"

---

### 3.2 `static/styles.css`

**변경 위치:** 파일 끝 `@media print{}` 블록 다음

**추가 내용 (418줄):**

1. **공통 헬퍼 클래스** (호주 기존 CSS에 없던 것들을 팀원 CSS에서 가져옴)
   - `.selector-row`
   - `.btn-analyze` (+ hover/disabled 상태)
   - `.result-card`, `.papers-card`, `.report-card` (+ `.visible` 활성 상태)
   - `.card-title`, `.card-title-dot`
   - `.basis-grid`, `.basis-item`, `.basis-label`, `.basis-value`

2. **2공정 전용 클래스** (팀원 원본 그대로)
   - `.p2-card`, `.p2-grid`
   - `.p2-mode-*` (모드 선택)
   - `.p2-tab-row`, `.p2-tab-btn` (탭 버튼)
   - `.p2-progress-text`, `#p2-ai-result-section`
   - `.p2-or-divider` (구분선)
   - `.p2-upload-*` (파일 업로드 UI)
   - `.p2-final-*` (최종 산정가 표시)
   - `.p2-seg-*` (공공/민간 세그먼트 버튼)
   - `.p2-brief-*` (보고서 요약)
   - `.p2-step-*` (단계 표시)
   - `.p2-removed`, `.p2-add-btn`, `.p2-custom-*`, `.p2-add-custom-btn`
   - `.p2-formula-*` (계산 공식 표시)
   - `.p2-scenarios`, `.p2-scenario` (시나리오 카드)
   - `.p2-ai-rationale-*`, `.p2-note`, `.p2-pdf-*`

3. **보조** 
   - `.report-loading-spinner` + `@keyframes spin` (로딩 스피너 애니메이션)

**색상 변수:** 호주 CSS에 이미 `--navy`, `--navy2`, `--red`, `--muted`, `--text`, `--card`, `--inner`, `--sh`, `--sh2`, `--sh3`가 전부 정의되어 있어서 팀원 CSS가 그대로 작동함.

---

### 3.3 `static/app.js`

**변경 위치:** 파일 끝

**구조:**

```
1~938줄     : 호주 원본 JS 그대로 유지
954줄~      : [호환 shim(매개층) 시작]
              - _escHtml      → _escapeHtml 로 위임
              - _loadReports  → reportStore 로 위임  
              - _setText      → 신규 정의
978~1644줄 : [팀원 원본 p2 JS 블록 — 수정 없이 그대로]
1645줄     : [팀원 원본 p2 JS 블록 끝]
1647~1653줄: DOMContentLoaded 업데이트
              → initP2Strategy() 호출 추가
```

**호환 shim(매개층)이 필요한 이유:**

팀원 코드와 호주 기존 코드에서 같은 역할을 하는 함수 **이름이 다름**. 팀원 코드를 한 글자도 안 건드리기로 했으니, 팀원이 쓰는 이름을 호주 코드의 이름으로 "연결만" 해주는 얇은 래퍼(wrapper)를 넣음.

| 팀원이 쓰는 이름 | 호주 원본 이름 | shim 처리 |
|---|---|---|
| `_escHtml` | `_escapeHtml` | 팀원 이름을 호주 이름으로 연결 |
| `_loadReports` | `reportStore` | 팀원 이름을 호주 보고서 저장소로 연결 |
| `_setText` | (없음) | 신규 정의 (DOM에 텍스트 쓰는 보조 함수) |

`typeof` 가드로 감싸놔서, 호주 원본 함수가 있을 때만 shim이 붙음. 호주 원본을 덮어쓰지 않음.

**이식된 팀원 JS 함수 25개 (v2.0 리네임 1개 포함):**

`_makeP2Defaults`, `initP2Strategy`, `switchP2Tab`, `setP2AiSeg`, `handleP2FileSelect`, `_showP2AiStatus`, `_resetP2AiResultView`, `runP2AiPipeline`, `_pollP2AiPipeline`, `_renderP2AiResult`, `_p2FillExchangeRate`, `_p2FillBaseFromReport`, `_syncP2ReportsOptions`, `_getP2SelectedReport`, **`_extractAudHint`** (← `_extractSgdHint` 리네임), `_renderP2ReportBrief`, `_calcP2Manual`, `_renderP2Manual`, `_p2OptionCardHtml`, `_bindP2OptionEvents`, `_renderP2CustomAddSection`, `_p2ManualScenarioReason`, `_p2ScenarioHtml`, `_renderP2PdfSection`, `_generateP2Pdf`

---

## 3.5. v2.0 호주화 치환 상세 (이번 작업)

### 화폐 단위
| 이전(싱가포르) | 이후(호주) | 위치 |
|---|---|---|
| `SGD` (23회) | `AUD` (23회) | 전 파일 전 구간 |
| `환율 (USD→SGD)` | `환율 (USD→AUD)` | _makeP2Defaults 공공/민간 각 1회 |
| `'USD 입력 시 적용, SGD면 1.0 유지'` | `'USD 입력 시 적용, AUD면 1.0 유지'` | hint 문구 |

### 세율 (GST)
| 이전(싱가포르 9%) | 이후(호주 10%) | 위치 |
|---|---|---|
| `value: 9, min: 9, max: 9` | `value: 10, min: 10, max: 10` | GST 옵션 |
| `label: 'GST 공제 (÷1.09)'` | `label: 'GST 공제 (÷1.10)'` | 필드 라벨 |
| `hint: '싱가포르 GST 9% 고정'` | `hint: '호주 GST 10% 고정'` | 안내 문구 |
| `price /= 1.09; parts.push('÷ 1.09')` | `price /= 1.10; parts.push('÷ 1.10')` | _calcP2Manual 계산 |
| `valDisplay = '÷ 1.09 고정'` | `valDisplay = '÷ 1.10 고정'` | 카드 표시 |

### 공공조달 채널
| 이전(싱가포르) | 이후(호주) | 위치 |
|---|---|---|
| `공공 시장: ALPS 조달청 채널 · 27개 공공기관 통합구매 기준` | `공공 시장: PBS 공공급여 채널 · 주별 병원조달(HealthShare NSW 등) 기준` | setP2AiSeg() + index.html |
| `민간 시장: 병원·약국·체인 채널 중심 유통 구조 기준` | `민간 시장: Chemist Warehouse 등 약국 체인 · 소매 유통 구조 기준` | setP2AiSeg() |

### 데이터 키 (백엔드 API 응답 포맷)
| 이전(싱가포르) | 이후(호주) | 영향 |
|---|---|---|
| `extracted.ref_price_sgd` | `extracted.ref_price_aud` | AI 추출 가격 |
| `analysis.final_price_sgd` | `analysis.final_price_aud` | 최종 산정가 |
| `scenarios[].price_sgd` | `scenarios[].price_aud` | 시나리오 가격 |
| `rates.sgd_krw` | `rates.aud_krw` | 환율 원/AUD |
| `rates.sgd_usd` | `rates.aud_usd` | 환율 USD/AUD |

> ⚠️ Jisoo 백엔드 API(`/api/p2/*`) 구현 시 위 키로 JSON 응답을 보내야 프론트가 정상 표시됩니다.

### 함수명 & 정규식
| 이전 | 이후 | 설명 |
|---|---|---|
| `function _extractSgdHint(text)` | `function _extractAudHint(text)` | 보고서 가격 추출 |
| `/SGD\s*([0-9]+)/` | `/(?:AUD\|A\$\|\$)\s*([0-9]+)/` | Chemist Warehouse는 `$` 표기 많아 함께 인식 |
| `const sgdUsd = Number(rates.sgd_usd)` | `const audUsd = Number(rates.aud_usd)` | 환율 변수 |
| `const usdToSgd = ...` | `const usdToAud = ...` | 역환율 |
| `<option value="abs_add_custom">SGD 가산</option>` | `<option value="abs_add_custom">AUD 가산</option>` | 커스텀 옵션 |

---

## 4. 백엔드 API (Jisoo가 구현 예정)

팀원 JS가 호출하는 API 엔드포인트(서버 주소) 목록. 호주 프로젝트에는 아직 없음.

| 엔드포인트 | 역할 |
|---|---|
| `POST /api/p2/upload` | 1공정 보고서 PDF 업로드 |
| `POST /api/p2/pipeline` | AI 가격 분석 파이프라인 시작 |
| `GET /api/p2/pipeline/status` | 파이프라인 진행 상태 조회 (폴링) |
| `GET /api/p2/pipeline/result` | 최종 결과 조회 |
| `GET /api/p2/report` | 저장된 보고서 목록/상세 |
| `GET /api/exchange` | 실시간 환율 |
| `GET /api/report/download` | PDF 다운로드 |

**연결 방식:** Jisoo가 이전에 받은 stage2(2공정) 백엔드 작업본과 합쳐서 `render_api.py`(또는 해당 라우터)에 엔드포인트 구현. 팀원 JS가 보내는 요청/응답 포맷을 확인하면서 호주 8개 품목(Hydrine, Sereterol, Omethyl, Rosumeg, Atmeg, Ciloduo, Gastiin CR, Gadvoa) 로직과 연결.

---

## 5. 검증 결과 (v2.0 재검증)

| 항목 | 결과 |
|---|---|
| `app.js` Node 문법 검사 | ✅ 통과 |
| 팀원 p2 함수 25개 존재 여부 | ✅ 전부 확인 (_extractAudHint 리네임 포함) |
| HTML `id="p2"` 블록 마커 | ✅ 5개 매칭 |
| CSS `.p2-*` 클래스 매칭 수 | ✅ 96개 |
| 호주 기존 UI(메인/1공정/3공정/보고서) 보존 | ✅ 미변경 |
| **app.js 싱가포르 잔여물 (`SGD`/`ALPS`/`1.09`/`조달청`)** | ✅ **0건** |
| **index.html 싱가포르 잔여물** | ✅ **0건** (이식 주석 제외) |
| **app.js `AUD` 등장 횟수** | ✅ **23회** |
| **index.html `AUD` 등장 횟수** | ✅ **7회** |
| **app.js `PBS/Chemist Warehouse/HealthShare` 등장** | ✅ **14회** |

---

## 6. 추후 Jisoo가 해야 할 일 (참고)

1. **백엔드 API 7개 구현** (`render_api.py` 등)
   - Jisoo가 가진 2공정 백엔드 작업본과 합치면서
   - 호주 PBS/TGA 가격 로직 연결
   - **응답 JSON은 반드시 `_aud` 키로 보내야 함** (`ref_price_aud`, `final_price_aud`, `price_aud`, `aud_krw`, `aud_usd`)

2. ~~싱가포르 → 호주 국가 문자열 치환~~ → **v2.0에서 완료됨** ✅

3. **1공정 보고서** 포맷과 `/api/p2/report` 응답 스키마 맞추기
   - 팀원 JS `_p2FillBaseFromReport`가 보고서에서 가격 정보를 뽑아 씀
   - 호주 AEMP/DPMQ 값을 어떤 key로 제공할지 확인 필요
   - `_extractAudHint`가 `AUD 12.34`, `A$12.34`, `$12.34` 패턴을 인식하므로 보고서 가격 힌트를 이 포맷 중 하나로 보내면 자동 추출됨

4. ~~**(선택) 품목별 GST 자동 전환 로직**~~ → **v2.1에서 완료됨** ✅
   - **처방약(Hydrine·Sereterol·Gadvoa·Rosumeg·Atmeg·Ciloduo·Gastiin CR)**: GST 0% (S4/S8 GST-free)
   - **OTC(Omethyl)**: GST 10%
   - 구현 위치: `_p2ClassifyGst(report)` + `_p2ApplyGstForReport(report)` 신규 함수
   - 작동 방식: 보고서 선택 시 `_p2FillBaseFromReport`가 `_p2ApplyGstForReport` 호출 → GST 카드의 `value`/`min`/`max`/`label`/`hint`/`enabled` 자동 갱신
   - 계산 로직: `_calcP2Manual`의 `gst_fixed` 분기를 동적 rate(`opt.value`) 기반으로 변경 — rate=0이면 공제 생략, rate>0이면 `÷(1+rate/100)` 적용

5. ~~**(선택) 메인 화면 GST 줄 표시 개선**~~ → **v2.1에서 완료됨** ✅
   - `templates/index.html` 77번째 줄 한 줄짜리 10% 표시를 두 줄로 분리
   - 첫 줄: "GST · 처방의약품 (GST-free, 부가세 면제) — 0%" (녹색) · 품목 7개 명시
   - 둘째 줄: "GST · OTC·건강기능식품 (부가가치세 과세) — 10%" (주황) · Omethyl 명시

4. **PBAC 임상우월성 이슈**(Rosumeg/Atmeg), **Commercial Withdrawal**(Ciloduo), **병원조달루트**(Gadvoa/Omethyl), **함량·제형 불일치 confidence 0.6**(Omethyl) 등 호주 특유 플래그를 2공정 결과 UI에 어떻게 노출할지 — 팀원 UI에는 없는 부분이므로 추가 설계 필요

---

## 7. 롤백 방법 (필요 시)

`app.js`만 백업 존재:
```
cp static/app.js.bak.before-p2-inject static/app.js
```

---

## 8. v2.1 GST 정밀화 상세 (2026-04-16 20:30 KST)

### 8.1 `templates/index.html` — 메인 화면 GST 표시 분리 (77줄)

**이전 (1줄):**
```html
<div class="trow"><div class="irow-row"><div><div class="tit">GST (부가가치세)</div><div class="sub">처방의약품 면제 · OTC 일부 과세</div></div><span style="...color:var(--warn);">10%</span></div></div>
```

**이후 (2줄):**
- 처방약 줄: 녹색 `0%` + 부제 "Hydrine · Sereterol · Gadvoa · Rosumeg · Atmeg · Ciloduo · Gastiin CR"
- OTC 줄: 주황 `10%` + 부제 "Omethyl (Omega-3) — 민간 유통 시 표시가에 포함"

### 8.2 `static/app.js` — 품목별 GST 자동 전환 로직

**신규 함수 (1274줄 근처 추가):**
```js
function _p2ClassifyGst(report) {
  // report 또는 문자열에서 제품명 추출 → OTC 여부 판정
  // Omethyl / omega-3 / 오메가 / omacor 키워드 매칭 시 OTC(10%)
  // 그 외 전부 Rx(0%, GST-free)
}

function _p2ApplyGstForReport(report) {
  // _p2Manual.private의 'gst' 항목을 classify 결과에 맞게 갱신
  // Rx(0%): value=0, enabled=false (공제 카드 비활성화)
  // OTC(10%): value=10, enabled=true, 공제 적용
}
```

**`_p2FillBaseFromReport` 수정:** 가격 힌트 채우기 이후 `_p2ApplyGstForReport(report)` 호출 추가.

**`_calcP2Manual` gst_fixed 분기 개선:** 하드코딩된 `÷ 1.10` 제거, `opt.value` 기반 동적 계산.
```js
} else if (opt.type === 'gst_fixed') {
  const rate = Number(opt.value) || 0;
  if (rate > 0) {
    const divisor = 1 + rate / 100;
    price /= divisor;
    parts.push(`÷ ${divisor.toFixed(2)} (GST ${rate}%)`);
  } else {
    parts.push('GST 면제 (처방약)');
  }
}
```

**`_p2OptionCardHtml` valDisplay 분기 개선:** 고정 카드 표시 문구도 동적.
- Rx(0%): `GST 면제 (처방약 0%)`
- OTC(10%): `÷ 1.10 고정 (GST 10%)`

### 8.3 테스트 시나리오 (배포 후 확인)

| 보고서 선택 | 민간 시장 GST 카드 | 계산 공식 예시 |
|---|---|---|
| Hydrine / Sereterol / Gadvoa / Rosumeg / Atmeg / Ciloduo / Gastiin CR | `GST 면제 (처방약 0%)` · 비활성화 | 공식에서 GST 단계 생략 |
| Omethyl | `÷ 1.10 고정 (GST 10%)` · 활성화 | `AUD n.nn × ... ÷ 1.10 (GST 10%) × ...` |

### 8.4 JS 문법 검증
- `node -c static/app.js` → ✅ 통과
- 싱가포르 잔재(SGD/ALPS/9%) 재검사 → ✅ 0건
- 호주 키워드(AUD/GST/PBS/Chemist Warehouse/HealthShare) → 48건

### 8.5 원본 UI/UX 작업본 반영 필요 항목 (메모)
`Australia_1st_logic` 폴더는 작업 복사본이므로 아래 변경사항을 원본 UI/UX 작업본에 추후 반영해야 함:
- `templates/index.html` 77줄 GST 표시 분리 (2줄로)
- `static/app.js` `_p2ClassifyGst` / `_p2ApplyGstForReport` 함수 추가
- `_p2FillBaseFromReport`, `_calcP2Manual`, `_p2OptionCardHtml` 3곳 수정

---

## 9. v2.2 직접 입력 탭 파일 업로드 통일 (2026-04-16 21:00 KST)

### 9.1 배경
Jisoo 요청: "직접 입력 페이지에 보고서 선택은 있는데 파일 업로드가 빠짐. 그거 AI 파이프라인 페이지랑 통일해."

### 9.2 `templates/index.html` — 직접 입력 탭 업로드 블록 추가
- 기존: `보고서 선택 <select>` + `보고서 브리프` 만 존재
- 추가: AI 탭과 동일한 `또는 PDF 직접 업로드` 구분선 + `<input type="file">` + 업로드 상태 영역
- 새 ID(AI 탭과 충돌 방지):
  - `p2-manual-upload-area`, `p2-manual-pdf-file`, `p2-manual-upload-text`, `p2-manual-upload-status`

### 9.3 `static/app.js` — 독립 핸들러 추가
- 새 상태: `let _p2ManualUploadedFilename = '';` (AI 탭의 `_p2UploadedReportFilename`과 독립)
- 새 함수: `handleP2ManualFileSelect(inputEl)` — AI 탭 핸들러 미러링, 세 가지 차이:
  1. `/api/p2/upload` 성공 시 `_p2ManualUploadedFilename` 저장 (AI 탭 상태는 건드리지 않음)
  2. 보고서 select 값을 '' 로 해제
  3. 파일명에서 품목 유추 → `_p2ApplyGstForReport(file.name)` 호출 → GST 자동 전환
- 보고서 선택 이벤트 수정: 보고서를 고르면 역으로 업로드 파일 상태 해제 (AI 탭과 동일 패턴)

### 9.4 `render_api.py` — 업로드 백엔드 스텁 신규
- 현재 작업 폴더의 render_api.py에는 `/api/p2/*` 엔드포인트가 하나도 없었음 (백업본에만 존재)
- 최소 스텁 2종 추가:
  - `POST /api/p2/upload` — PDF 업로드 저장 (base64 디코딩 → `reports/_p2_uploads/` 저장, `{ok, filename, size_bytes}` 반환)
  - `GET /api/p2/pipeline/status` — 대기 상태 반환 (엔진 미구현 단계)
- 파일명 안전화: 영숫자·점·밑줄·하이픈 외 문자는 `_` 로 치환, 100자 초과 시 꼬리만 유지, 타임스탬프 prefix 부여
- 저장 위치: `reports/_p2_uploads/` (디렉토리 자동 생성)
- 업로드 용량 제한은 아직 없음 — 다음 단계에서 FastAPI body size 제한 추가 예정

### 9.5 사용자 플로우
1. 직접 입력 탭 진입
2. (방법 A) 저장된 1공정 보고서 선택 → 가격 힌트·GST 자동 반영
3. (방법 B) PDF 파일 업로드 → 파일명에서 품목 유추 → GST 자동 전환, 저장 확인 메시지
4. 민간/공공 시장 선택 → 옵션 조정 → PDF 생성

### 9.6 검증
- `python -c "import ast; ast.parse(...)"` → ✅ Python 문법 OK
- `node -c static/app.js` → ✅ JS 문법 OK

### 9.7 원본 UI/UX 작업본 추가 반영 필요
- `templates/index.html` 직접 입력 탭 업로드 블록 HTML
- `static/app.js` `handleP2ManualFileSelect` 함수, `_p2ManualUploadedFilename` 상태, 보고서 select 이벤트 변경
- `render_api.py` `/api/p2/upload` + `/api/p2/pipeline/status` 스텁 (임시 — 다음 단계에서 AI 엔진 구현)

HTML/CSS는 Edit 도구로 부분 수정이라 `.bak`이 없음. 롤백 필요하면 Git 커밋 되돌리기 권장.

---

## 10. Stage2 FOB 백엔드 이식 (v2.3 · 21:30 KST)

### 10.1 요청 배경
- 작업 폴더(`Australia_1st_logic/upharma-au`)에는 `stage2/fob_calculator.py` 와 `stage2/fob_reference_seeds.json` 파일은 있었지만 `render_api.py` 에 FastAPI 엔드포인트가 빠져 있었음.
- 백업 폴더(`Australia_1st_logic - claudecoworkonly_BACKUP_20260416_1829/upharma-au`)에는 이미 구현된 `/api/stage2/seeds` 와 `/api/stage2/calculate` 엔드포인트가 존재.
- Jisoo 결정: "백업에서 stage2 백엔드 먼저 가져오기 (추천)" → 그대로 이식.

### 10.2 이식 범위 (render_api.py)
| 블록 | 라인 (작업본) | 내용 |
|---|---|---|
| Stage2 섹션 주석/import | 1068–1089 | `stage2.fob_calculator` 에서 6개 심볼 지연 import (`_STAGE2_OK` 플래그) |
| 경로 상수 | 1091–1093 | `_STAGE2_SEEDS_PATH`, `_AU_PRODUCTS_PATH` |
| 헬퍼 함수 | 1095–1123 | `_load_stage2_seeds()`, `_load_au_products_meta()` |
| `GET /api/stage2/seeds` | 1126–1170 | 8품목 시드 목록 — UI 드롭다운용 컴팩트 필드. `product_name` 은 `au_products.json` 에서 보강. |
| `_seed_by_id`, `_scenarios_dict_to_list` | 1173–1204 | 내부 헬퍼 |
| `POST /api/stage2/calculate` | 1208–1392 | Manual 탭 계산 — Logic A/B, 3 시나리오(aggressive/average/conservative), withdrawal 차단, PBAC/hospital/confidence 경고 |
| `POST /api/p2/pipeline` (501) | 1463 | AI 파이프라인 실행 스텁 — Haiku 연동 전 |
| `GET /api/p2/pipeline/result` (501) | 1469 | AI 파이프라인 결과 스텁 |
| `POST /api/p2/report` (501) | 1474 | PDF 생성 스텁 |

### 10.3 백업 원본 대비 변경점 (호주화·한글 라벨 보강)
- 백업의 PBAC/S19A/Restricted/hospital 경고 문구에 **한국어 괄호 설명 병기** (프로젝트 규칙: 축약 용어는 풀어서 표기):
  - `PBAC 임상우월성` → `PBAC(호주 의약품급여자문위원회) 임상우월성`
  - `Hospital tender` → `Hospital tender(병원 공급 입찰)`
  - `Section 19A` → `Section 19A(일시수입 특례)`
  - `PBS Restricted Benefit` → `PBS Restricted Benefit(처방 적응증 제한)`
- `except ValueError as e: ... from e` — 예외 체이닝 명시 (PEP 3134)
- 기존 `/api/p2/upload`(실 동작), `/api/p2/pipeline/status`(Haiku 대기 메시지)는 백업 스텁보다 구현이 앞서 있어 **보존**.

### 10.4 검증
```
$ python -c "import ast; ast.parse(open('render_api.py').read())"   → SYNTAX OK (1507 lines)
$ python -c "from stage2.fob_calculator import ..."                 → STAGE2 imports OK (DEFAULT_FX=900.0)
$ TestClient GET  /api/stage2/seeds                                 → 200, count=8
$ TestClient POST /api/stage2/calculate (Hydrine / Logic A)         → 200, 3 시나리오 (FOB AUD 29.0/26.6/24.5)
$ TestClient POST /api/stage2/calculate (Omethyl / Logic B)         → 200, 3 시나리오 (FOB AUD 27.1/25.9/23.9)
$ TestClient POST /api/stage2/calculate (Ciloduo / withdrawal)      → 200, logic=blocked, scenarios=0, reason=commercial_withdrawal
$ TestClient POST /api/p2/pipeline                                  → 501 (예정된 stub 동작)
```

### 10.5 8품목 시드 매핑 확인
```
au-omethyl-001    ESTIMATE_private     (Omethyl Cutielet)
au-gadvoa-002     ESTIMATE_hospital    (Gadvoa — 병원조달 전용)
au-sereterol-003  DIRECT               (Sereterol)
au-hydrine-004    DIRECT               (Hydrine)
au-rosumeg-005    COMPONENT_SUM        (Rosumeg — PBAC 임상우월성 필요)
au-atmeg-006      COMPONENT_SUM        (Atmeg  — PBAC 임상우월성 필요)
au-ciloduo-007    ESTIMATE_withdrawal  (Ciloduo — 2021 commercial withdrawal)
au-gastiin-008    ESTIMATE_substitute  (Gastiin CR)
```

### 10.6 원본 UI/UX 작업본 추가 반영 필요
- `render_api.py` — Stage2 섹션 전체 (import/helpers/`/api/stage2/seeds`/`/api/stage2/calculate`) + p2 stub 3종 추가
- `stage2/fob_calculator.py`, `stage2/fob_reference_seeds.json` — 이미 작업본에 존재 (백업과 동일본)

### 10.7 다음 단계
1. Supabase 스키마 설계안 (Cursor 프롬프트) — 2공정 계산 결과 저장 테이블
2. Haiku 기반 AI 파이프라인(`POST /api/p2/pipeline`) 구현 — 업로드 PDF → Haiku 추출 → `fob_calculator` 실행 → 결과 반환
   - **모델 고정:** `claude-haiku-4-5-20251001` (절대 Sonnet/Opus 사용 금지)
3. PDF 생성(`POST /api/p2/report`) 실구현
4. 배포 URL 확인 (최종 단계)
