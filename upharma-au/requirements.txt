# Render 서비스 Root Directory 가 upharma-au 인 경우에만 이 파일이 빌드에 포함된다.
# 정본(관례)은 저장소 루트의 requirements.txt 이다. 의존성 변경 시 반드시 루트와 동일하게 맞출 것.
httpx
selectolax
trafilatura
supabase
python-dotenv
tenacity
openai
fastapi
uvicorn[standard]
jinja2
anthropic
pydantic
reportlab>=4.0
yfinance>=0.2.36
